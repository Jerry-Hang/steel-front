# .ds-memory.md — 已作废（2026-09-26）

**不要照这个文件做事。** 它是 2026-09-01 那次会话的记忆快照，内容已被
`AGENTS.md`（仍然生效的约束 / 未结案 / 教训）与 `docs/PROGRESS.md`（进度与时间线）取代。

作废的直接原因：**里面有过期甚至相反的常量**，而它躺在仓库根目录、最容易被先读到 ——
"过期的待办"和"错的结论"一样有害（AGENTS 铁律 F）。已核实的矛盾：

| 本文件里的写法 | 现在的事实（判据） |
|---|---|
| `PT_MAX_BOXES=512` | **1024**（`rg --fixed-strings "const PT_MAX_BOXES"`，2026-09-14 扩容） |
| 常驻 PT 图像格式 = `B8G8R8A8_UNORM` | **`R8G8B8A8_UNORM`**，必须与 GLSL 的 `rgba8` 逐位相等（铁律 B PT 段；"兼容"不算数） |
| "D11 仍未修复" | GLB 立面的重影已由 `Shape::Authored` → `flat_flag=1.25` 收口（铁律 B 顶点格式段） |
| "405 tests 全绿、警告 46" | 当前 `cargo test --release` 见 `docs/PROGRESS.md`（0 警告是硬红线） |
| 待办 ① 删 `pt_live_frame`/`pt_live_frame2` | **两个函数都已不存在**（`rg pt_live_frame src/` 无输出） |
| 待办 ④ `PT_SUN_AMBIENT` 无消费者 | 该符号**已从全仓消失**（含 GLSL） |
| 待办 ⑦ D4 墙缝天空亮条 | 已结案：数值巡检判定非缺陷（未结案清单 #8） |

当时仍有效、现已归档的一条：**PT 曝光仍硬编**（`main.rs` 的 `exposure: 0.4` 是标定值，
未进 `config.rs` / 设置面板）—— 已补进 AGENTS 未结案 #11。

保留本文件（而不是删除）只是为了留下这个指针：**会话记忆请写进 `docs/PROGRESS.md`，
仍然生效的约束请写进 `AGENTS.md`**，不要在仓库里另开第二份"记忆"。
