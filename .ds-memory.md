# .ds-memory.md

## 会话状态
- 已解决问题：**PT 全图无命中（根因 = rayQuery getter 的 committed 参数传 0 取候选记录）**；PT 着色器弃手工 SPIR-V 改 glslang 编译；PT 场景与光栅化 WorldMarker 同源；record_pt_build scratch 每调用泄漏 2MB + 双构建无 barrier；BLAS 按当前盒数而非容量分配导致 device lost；盒面法线主轴误判导致地面全黑；检视白屏；RV3D_VALIDATION=1 门控；RV3D_PT_LIVE=0 通道；D1 阵营色（dad7aef）；D3 阴影二重映射（df86274）；D2 玻璃幕墙（46f689a）；D6 圆柱四肢/球形头（f280351）；D5 爆炸圆润化（3d7507d）
- 验收实测：405 tests 全绿；警告 46（bin）+1（build script），较存量 49 减 3、0 新增；冒烟 ALL-OK（VUID=0 kills=9 fps 130.8–193.2 panics=0）；PT-VIEW 256² 非天空 61.8% 含接触阴影；PT-LIVE 512² 实跑 fps 181–182、PT-SCENE 盒 512、无 device lost
- 新引入风险：`PT_MAX_BOXES=512` 是硬上限且**静默截断**（实测 marker=547 > 512，超出部分不进 PT 场景）；BLAS/scratch 尺寸按容量算 → 改 `PT_MAX_BOXES` 会同时放大显存占用；`pt_set_scene_markers` 内 `device_wait_idle` 是**整设备停顿**，只可在集合真变时触发（指纹判定勿删）；盒面法线依赖 `box_triangles`/`box_indices` 的 6 面×2 三角展开序（改三角化布局必须同步改 GLSL 面号表）
- 下一步建议：① PT 降噪（当前 1 spp + 哈希余弦采样，颗粒明显；加 spp 累积或蓝噪声/去噪——作烘焙参照必须先收敛）② 删 `renderer.rs` 死函数 `pt_live_frame` / `pt_live_frame2`（无调用点且描述符缺 binding 2，误接必崩；顺带清其贡献的告警）③ 提 `PT_MAX_BOXES` 或按视锥裁剪以覆盖 547 marker ④ 天空/环境光接进 `light_uniform`（`PT_SUN_AMBIENT` 现仍无消费者）⑤ 曝光/弹跳数进 config.rs 持久化（现曝光硬编 0.2 于 main.rs）⑥ PT 与光栅同屏叠加（现 PT 直接替换画面，只能当调试视图）

## 关键约定（勿回退）
- **rayQuery getter 第二参数 = `committed`，必须 `true`**：`false/0` 取候选记录，proceed 结束后恒为 NoIntersection → 全图无命中（两天悬案真因）
- PT 着色器一律 `assets/rt/pt_panorama.glsl` --glslang--> `.spv`（跑 `scripts/compile_pt.ps1`），**禁止手工拼装 SPIR-V**；build.rs 在 GLSL 比 SPV 新时发 cargo:warning
- PT 接口：binding 0=TLAS / 1=storage image(rgba8) / 2=每盒材质 SSBO；push constants 5×vec4=80B（`PtParams::pack` 与 GLSL `PC{a,b,c,d,e}` 逐字段同步）；相机**直传 `camera.forward()`**，不在着色器重推 yaw/pitch
- PT 场景 = 光栅化同一批 `WorldMarker`（平移=中心、缩放列长/2=半宽、tint=反照率，盒 0=地面）；FNV 指纹（坐标量化 1m）判定真变化才重建
- AS 容量铁律：顶点/索引/材质缓冲 + **BLAS 尺寸与 scratch** 全按 `PT_MAX_BOXES` 算；TLAS 恒单实例（全盒合进同一 BLAS）
- scratch 归 `PtAssets`：BLAS 前段 / TLAS 后段，两次构建间必须 `ACCELERATION_STRUCTURE_BUILD` barrier
- 盒面法线 = `(primitive % 12) / 2` 查表（-X,+X,-Y,+Y,-Z,+Z），**勿用「来射方向主轴」近似**
- 常驻 PT 图像格式 = `B8G8R8A8_UNORM`（匹配 swapchain blit 通道序）；`run_pt_view` 仍用 R8G8B8A8 出 BMP
- 阴影深度 = clip.z 原值（glam ortho_rh 本版本产 [0,1]），禁止 *0.5+0.5
- NPC 片元 = tint × (0.55+0.9·luma)；玻璃 marker b>r·1.4 跳过混凝土纹理
- mesh 几何槽位：盒 [66561,69633) CUBE / 四肢 [69633,72705) 圆柱 / 头 [72705,75777) 归一 ICO / 自发光 [75777,75841) SPH / 枪 75841（mesh 不画）
- MeshOutput 50v/96p，workgroup 96；RV3D_DEBUG_SHADOW=1 为常驻阴影诊断
- 游玩/巡检默认 `RV3D_PT_LIVE=0`（PT 会替换整个画面，当前是 RT 参照视图而非玩法渲染）
