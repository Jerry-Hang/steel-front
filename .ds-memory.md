# .ds-memory.md

## 会话状态
- 已解决问题：检视模式白屏（c5b7c29 已并入工作区）；验证层改 RV3D_VALIDATION=1 门控；PT_LIVE=0 关闭通道；D1 NPC 阵营色（dad7aef）；D3 阴影深度二重映射根治（frag_depth=sp.z，旧 *0.5+0.5 偏移 +0.25 致全场误判阴影/零对比，同日修复 world_to_shadow_uv 镜像与测试期望）
- 新引入风险：renderer.rs 工作区含另一会话未提交的 PT WIP 之外的改动须逐次核对；RV3D_DEBUG_SHADOW=1 诊断（灰度/深度/矩阵 dump）已常驻，默认关闭零成本
- 下一步建议：D2 建筑窗户乱码 → D6 NPC mesh 几何退化 → D4 悬浮亮条 → D5 爆炸尖刺；PT 命中问题归另一会话主线

## 本次改动（src/main.rs）
- 环境变量 RV3D_PT_LIVE 语义扩展：0=强制关闭 PT 全景，1=强制开启，未设置=跟随 config.pt_enable
- 新增注释说明三态语义
- 未改动 config.rs、ui.rs、renderer.rs

## D3 诊断与修复要点
- 证据链：矩阵 dump（col3.z=0.499）+ 调试图（R=frag_depth≈0.74 vs G=d_avg≈0.49，恒差 +0.25）
- 修复：build.rs frag_depth = sp.z；lighting.rs (uv, p.z)；测试期望改 (0.4,0.6) 区间
- 验收：405 tests 绿；树木/建筑地面投影截图可见，方向正确，场景亮度分层恢复
