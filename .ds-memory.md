# .ds-memory.md

## 会话状态
- 已解决问题：**PT 全图无命中（根因 = rayQuery getter 的 committed 参数传 0 取候选记录）**；PT 着色器弃手工 SPIR-V 改 glslang 编译；PT 场景与光栅化 WorldMarker 同源；record_pt_build scratch 每调用泄漏 2MB + 双构建无 barrier；BLAS 按当前盒数而非容量分配导致 device lost；盒面法线主轴误判导致地面全黑；**PT 降噪 = spp 时域累积（线性 HDR 均值）已落地**；检视白屏；RV3D_VALIDATION=1 门控；D1–D3/D5/D6（渲染巡检三波）
- 验收实测（2026-09-01）：405 tests 全绿（spp 波未重跑）；警告 46（bin）+1（build script），存量 49 → 减 3、0 新增；冒烟 ALL-OK（VUID=0 kills=9 fps 130.8–193.2，命中根治波）；`RV3D_PT_SPP=128` 参考帧 256² 噪点目视消失 + 接触阴影软半影；`RV3D_PT_SPP=512` 实机 2560×1600 干净收敛画面、无 device lost、收敛后 fps 184.9
- 新引入风险：`PT_MAX_BOXES=512` 硬上限且**静默截断**（实测 marker=547）；push constants 现为 **96B（6×vec4）**，改 PC 块必须同步 `PtParams::pack` + 两处 `.size(96)` + GLSL，任一不改 = 静默错相机/错累积；累积状态是 `Cell`（`record_command_buffer` 是 `&self`）；BLAS/scratch 按容量算，改 `PT_MAX_BOXES` 会同步放大显存占用；`pt_set_scene_markers` 内 `device_wait_idle` 是**整设备停顿**，只能靠指纹判定触发（勿删该判定）
- 下一步建议：① 删 `renderer.rs` 死函数 `pt_live_frame`/`pt_live_frame2`（2-binding 旧描述符，误接必崩；顺带清其告警）② 提 `PT_MAX_BOXES` 或按视锥裁剪以覆盖 547 marker ③ 曝光/弹跳/spp 进 config.rs + 设置面板（现曝光 0.2 硬编 main.rs）④ 天空/环境项接进 `light_uniform`（`PT_SUN_AMBIENT` 仍无消费者）⑤ 移动相机时改为按像素重投影复用或运动自适应 spp（现每次全量重开）⑥ 补跑 `cargo test --release` + 冒烟（spp 波未跑）⑦ D4 墙缝天空亮条、mesh 严格 spirv-val 布局

## 关键约定（勿回退）
- **rayQuery getter 第二参数 = `committed`，必须 `true`**：`false/0` 取候选记录，proceed 结束后恒为 NoIntersection → 全图无命中（两天悬案真因）；排查顺序是先反汇编看该指令第二操作数是 `%int_1` 还是 `%uint_0`，再动 AS
- **PT 降噪靠 spp 时域累积，但采样种子必须含帧索引**：种子只由 `gid` 决定时逐帧图案静止、累积永不收敛。acc 图像 = RGBA32F（rgb=Σ线性样本、a=spp），ACES+linear→sRGB **只作用于 `acc.rgb/acc.a` 均值**（先各自 tonemap 再平均会压平高光）
- **累积图像布局纪律**：只在创建时做一次 `UNDEFINED→GENERAL`，逐帧 barrier 必须 `GENERAL→GENERAL`；对它用 `old_layout=UNDEFINED` = 告诉驱动内容可丢 = 累积白做，且**不报 VUID**、只表现为"降噪无效"
- 复位累积的三条路：取景指纹 `PtParams::signature()` 变化 / `pt_set_scene_markers` 重建 / `destroy_pt_resident`
- 累积达标（`pt_frame >= pt_spp_target`）后**跳过整个 dispatch**，帧率回满；`RV3D_PT_SPP` 覆盖（实时默认 256、PT_VIEW 默认 64）
- PT 着色器一律 `assets/rt/pt_panorama.glsl` --`scripts/compile_pt.ps1`(glslang)--> `.spv`，须过严格 `spirv-val --target-env vulkan1.3`；build.rs 嵌入 `PT_FRAME_SPV`，GLSL 比 SPV 新会发 cargo:warning；**禁止手工拼装 SPIR-V**
- PT 接口：binding 0=TLAS / 1=rgba8 storage image / 2=每盒材质 SSBO / 3=RGBA32F 累积图像；push constants 6×vec4=96B（a res/tan/bounces、b camPos、c fwd、d sunDir、e sunColor+exposure、f frame+reset+sppTarget）
- 相机**直传 `camera.forward()`**，不在着色器重推 yaw/pitch；PT 场景 = 光栅化同一批 `WorldMarker`（平移=中心、缩放列长/2=半宽、tint=反照率，盒 0=地面）
- AS 容量铁律：顶点/索引/材质缓冲 + **BLAS 尺寸与 scratch** 全按 `PT_MAX_BOXES` 算；TLAS 恒单实例（全盒合进同一 BLAS）；scratch 归 `PtAssets`（BLAS 前段/TLAS 后段）+ 两次构建间 `ACCELERATION_STRUCTURE_BUILD` barrier
- 盒面法线 = `(primitive % 12) / 2` 查表（-X,+X,-Y,+Y,-Z,+Z），**勿用「来射方向主轴」近似**（浅角度会把地面判成 ±Z → ndl<0 → 地面全黑）
- 常驻 PT 图像格式 = `B8G8R8A8_UNORM`（配 swapchain blit 通道序）；`run_pt_view` 仍用 R8G8B8A8 出 BMP
- **RT 通路声称"已验证"必须给命中着色图像证据**：`tools/rt-bench` 是逐线程无条件 `OpStore 1`、从不查 intersection，其 rays/s（33.6G）只是遍历吞吐，不构成命中证明
- **自发光 `tint.w` 现在是「火/烟」选择器（2026-09-01 起，勿当 alpha 用）**：`<0.5 = 火`（中心过曝、向外快衰），`>=0.5 = 烟`（暗灰、吃环境光、缓衰减）。`build.rs` 的 vs 与 mesh 两条路径同一判据；**任何新特效进自发光通道都必须显式给这个值**，且注意自发光槽位只有 64 个（与 `EMISSIVE_INSTANCE_BASE + 64` 同步），main.rs 侧策略是"爆炸保底 + 剩余按相机距离由近及远"
- **距离感知细节收敛（D12）**：`detail = 1 - smoothstep(0.015, 0.22, max(|fwidth(uv)|))`，近处与 D1 验收式 `0.55+0.9·luma` **逐位一致**、远处收到 `0.55+0.12·luma`；NPC 轮廓提亮只乘亮度标量（不改 RGB 比例，阵营色相结论仍有效）
- **D11 仍未修复（2026-09-01 二次判定）**：`city.rs::office_tower` 的窗带薄盒已删、片元已按世界 Y 画窗，但实机放大图显示悬挑 3D 板与 V 形干涉**照旧存在** → 画面里的楼不是 `office_tower` 生成的。**先 `grep -rn GLASS_BLUE src/` 找全部发射源**（含 city.rs 其它函数与 game.rs 程序化障碍环带），别再改 `office_tower`、别再叠着色器窗带**：薄盒在掠射角下自身前后面屏幕重合 → V 形锯齿深度干涉；把条带改薄方向是反的。正解是把窗带画进立面程序化纹理（按高度分层）
- **冒烟 fps_min 越线先判是否首帧窗口**：`npc` 计数远低于稳态 + `wait_fence_us ≈ frame_us` 即预热特征（build.rs 重新生成 SPIR-V 后驱动 shader JIT 冷缓存）；**重跑确认，不要调阈值**
- 阴影深度 = clip.z 原值（glam ortho_rh 本版本产 [0,1]），禁止 *0.5+0.5
- NPC 片元 = tint × (0.55+0.9·luma)；玻璃 marker b>r·1.4 跳过混凝土纹理
- mesh 几何槽位：盒 [66561,69633) CUBE / 四肢 [69633,72705) 圆柱 / 头 [72705,75777) 归一 ICO / 自发光 [75777,75841) SPH / 枪 75841（mesh 不画）；MeshOutput 50v/96p，workgroup 96
- RV3D_DEBUG_SHADOW=1 为常驻阴影诊断；游玩/巡检默认 `RV3D_PT_LIVE=0`（PT 整体替换画面，是参照视图不是玩法渲染）
- 仓库卫生：`scripts/.ds_*`（含 API key 明文）与 `data/` 已进 .gitignore，提交一律显式列路径、禁 `git add -A`
