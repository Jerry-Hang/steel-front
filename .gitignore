# 运行产物与诊断文件
*.bmp
*.png
*.log
*.err
GAME_DESIGN.txt
_fontblock.txt
target/
scripts/retain_smoke.txt
release_dist/
# scripts/package_release.ps1 的产物（含 exe + assets + zip，约 10MB）。
# 打包产物一律不入库 —— 需要分发时现跑脚本重建。
dist/
launcher/target/
# 会话构建/测试日志（重定向产物，非工程数据）
logs/

# 取证截图：2026-09-12 按用户决定从 git 移除（405 张 / 324 MB），文件保留在磁盘上，
# 只是不再随仓库分发。需要提交某张证据图时用 `git add -f <路径>`。
screenshots/

# AI 会话临时请求/响应体（含 API key 明文，绝不入库）
scripts/.ds_req.json
scripts/.ds_resp.json
scripts/.ds_out.txt

# 本地运行与诊断数据（战斗日志 / LLM 决策流 / 临时探针脚本）
data/

# Python 字节码缓存
__pycache__/
*.pyc

# 生成器的临时输出目录（Blender 产物先落这里，确认后才入库）
build/
