# 交接文档 · Steel Front · 2026-09-02 晚

> 交接给下一任模型/维护者。**先读这份，再看 README.md 与 git log**（每一轮关键决策都写进了 commit message）。

---

## 一、当前状态（可玩！）

- **master 已全部推送**，本地工作区仅剩构建产物（assets/*.spv）差异，可忽略。
- **光栅渲染 = 稳定可玩**：BAT 一键启动（fetch→build→launch），222fps @2560×1600。
- **路径追踪（全景 PT）已停用**（config pt_enable=false），原因见第四节。

## 二、本轮已完成的渲染质量修复（验证过截图）

1. **光照提升**：ambient 0.30→0.55、太阳 1.15→1.35（game.rs `light_uniform`）——阴影内场景不再死黑。
2. **mesh-shader 路径已禁用**（renderer.rs 1279 行 `mesh_enabled: false`）：
   - A/B 实锤：mesh 路径下**地面全黑**；传统 vertex 路径细节完整（树/围墙/路沿全亮），222fps 与 mesh 持平。
   - 根因未深挖（mesh shader 对"地面实例 quad"的几何/颜色错误），见"遗留问题 3"。

## 三、遗留问题（按优先级，每项都附了诊断结论）

### 1. ⭐ 黑色街区块 = 未绘制区（最高优先级）
- 现象：画面中街道/广场是**纯黑矩形块**（不是阴影/曝光/纹理——已三重排除：NO_SHADOW=1 仍黑；纹理 dump `ground_texture_dump.png` 完美；光照关/开同黑）。
- 本质：黑色区域没有三角形覆盖，露出 clear color（0.24,0.36,0.60 深蓝黑）。
- **修法建议**：
  a) 近/远地面 quad 实例（GROUND_VERTS）在街区处的实例缺失 → 检查实例生成/槽位数（near_count/far_count 与实际 block 数）；
  b) 或 terrain mesh 街区处有凹陷(LOD)未覆盖 → 简单兜底：主 pass 画一张全图地面 plane（512×512, 采样 `texture_sampled` 烘焙图, y=0.001 与 quad 拉开 depth 位）。
  c) 最快验证：在 terrain draw 前加一张全屏地面 quad（同烘焙纹理）——若街面出现纹理 = 确认"覆盖缺失"。

### 2. 路径追踪 PT 恢复（需 30 分钟）
- 症状：启动到 `PT-SCENE: 盒 512 个` 后 **0xC0000005**（ACCESS_VIOLATION，无 Rust panic = 驱动/原生层）。
- 已排除：驱动（重启后复现）、spv（spirv-val 通过 + git 原版）、shader 参数（SPP 16/64 正常版）。
- **嫌疑**：`Cargo.lock` 随 force-push 回退到旧版，依赖（ash 等）版本与昨天 93fps 构建不一致 → 库版本不匹配的 UB。**第一步试**：`git log --all -- Cargo.lock` 找到昨天正常构建时的 lock，恢复后 `cargo build --release` 复测。
- 恢复路径：`src/config.rs` pt_enable=true → 构建 → 若仍崩，比较 Cargo.lock。

### 3. mesh-shader 地面全黑（次优先）
- 禁用状态下不影响现玩版本；要修：比对 MESH_SHADER_WGSL（build.rs）中"地面实例 quad 几何生成"（GROUND_POS 常量在 mesh shader 中疑似从未被引用——已 grep 确认仅常量区出现）。

## 四、踩坑教训（勿重蹈）

| 坑 | 教训 |
|---|---|
| 白屏 = SPP 48/160 + bounces 8 能量爆白 | 改渲染参数必须截图验证；激进参数先小步 |
| BAT 自动 git pull | 本地改动若不同步 master，BAT 会拉回远端代码——改完必须 push |
| pt_panorama.spv 磁盘缓存 | 改 glsl 后必须删 spv 强制重编（build.rs stale 检测只看 mtime） |
| mesh 路径地面黑 | mesh 与传统路径 A/B 测，别信代码注释 |
| 65 帧/功耗 115W | 功耗墙=MSI 增强模式（硬件层）；FurMark 113W 证明 GPU 能到 |

## 五、关键环境/命令

- 工具链：Rust stable MSVC；Vulkan SDK `C:\VulkanSDK\1.4.357.0\Bin\`（glslang/spirv-dis/spirv-val）。
- 构建：`cargo build --release`（build.rs 自动编译 WGSL→SPIR-V + RT 基准）。
- 截图打游戏内验证：`powershell -ExecutionPolicy Bypass -File scripts\play_cap.ps1`（设置前台窗口焦点 + R 重开 + PrintWindow 存 `screenshots/play.png` + 鼠标复位）。
- 纹理 dump：`cargo test --release dump_city_ground_texture_and_report_zones` → `screenshots/ground_texture_dump.png`。
- 光栅调试 env：`RV3D_NO_SHADOW=1`（关阴影 A/B）、`RV3D_PROC_TEX=0`（用 test.png）、`RV3D_DEBUG_SHADOW=1`。
- 安全基线 commit：**35a49c6**（9-1 全景 PT 正常版，一切 PT 实验以此为锚）。

## 六、用户背景（重要）

- 机器：RTX 5060 Laptop 8GB + Ryzen 16C/32T + 2560×1600@144Hz；MSI 手动模式（功耗墙 55W 默认/115W 上限，FurMark 实测 113.6W）。
- 用户在意：画面真实性（阴影必须实时、无贴图感）、移动无叠影/噪点、功耗吃满、成本（模型 token）。
- 交互要求：**改完必须自己截图自己看**（`scripts/play_cap.ps1`），别让用户当小白鼠。
