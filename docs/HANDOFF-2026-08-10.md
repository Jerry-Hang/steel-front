# 交接文档 / HANDOFF — 2026-08-10（夜间会话收尾）

> 用途：下一会话从本文件 + `AGENTS.md` 快速恢复。先读 AGENTS.md，再读本文件。

## 1. Git 状态（本会话结束）
- 分支 `master`，远端 `origin`；HEAD = `2255498`；工作区干净
- 本会话 6 个 commit（基于 `02508cf`，均未推送）：
  - `f46c629` feat(render): 纹理 mip 链 + 各向异性采样（P0 锯齿修复）
  - `3994a69` docs: 光照渲染验证报告
  - `6b6845f` feat(terrain): 程序化地形高度（r≤140m 平坦，外围 ≤15m 丘陵）
  - `81fc9b4` feat(audio): 程序化 DSP 音效合成（枪声/爆炸/脚步/环境风）
  - `55af294` feat(game): AI 掩体利用 + 可破坏障碍 + 任务目标
  - `2255498` feat(net): UDP client/server 回环联网（RV3D_NET=server|client）
- ⚠️ 提交已完成，**尚未 push**；下一步 `git push origin master`
- git identity：`Evernight <3520143257@qq.com>`；一个功能一个 commit；git 只在 WSL 跑

## 2. 本会话干了什么（并行 6 Agent + 主线程集成）
- 6 个子 Agent 并行产出（mipmap/光照文档/地形/音频/玩法/联网），共享工作区直写；
  第 7 个（Vulkan 规划）因线程上限未 spawn，改为主线程撰写
- 集成期修复的编译/测试问题（重要，勿再犯）：
  - `handle_join` **内部已发送 ack**：调用方再 `send_to` 会重复 ack，
    导致客户端把第二个 ack 当快照处理。已统一为只调 `handle_join` 不再手动发送
    （net.rs 测试、game.rs `step_net_server`、demo `update_net` 三处）
  - `recv_until` 测试助手参数需 `mut recv: impl FnMut`
  - 音频钳位测试 `vec![0.0; 9600]` 需 `render(&listener, 4800, ...)`（9600=4800×2）
  - 闭环测试“追平快照”是竞态：UDP 环回有毫秒延迟，收尾改为 1s 内轮询直到
    `snapshot_seq == net_snap_seq`
- 验收：`cargo test` **254 全绿**、0 警告（已强制重编译确认）

## 3. 环境/沙箱须知（本会话实测）
- **UDP 回环测试在沙箱内 bind 直接 PermissionDenied（非代码问题）**：
  `cargo test` 必须提权跑（已批准前缀 `cd /home/jerry-huang/Rust/Rust_Vulkan_3D && cargo test ...`）
- 12GB 内存：一次只跑一个 cargo，禁止并行构建；子 Agent 只改代码不跑构建
- 冒烟（`scripts/run_gameplay_smoke.sh`）**本会话未跑**——需要 release 构建 + X11 包装
  （`unshare -rm` bind-mount），是下一会话第一项验证（重点看 VUID=0 与地平线锯齿是否消失）
- 备份：`/tmp/steel_backup_20260809/worktree_all.patch`（全量 diff）+ 光照文档副本；
  各 Agent 隔离补丁在 `/tmp/patch_*.patch`（mipmap/terrain/audio/gameplay/net/lighting_doc）

## 4. 各功能一句话说明
- mipmap：纹理 256×256→9 级，blit 生成；sampler min/maxLod + anisotropy（feature 支持才开）
- 地形：`terrain_height`/`terrain_height_at` 同源确定性值噪声；`TERRAIN_FLAT_RADIUS=140` 内恒 0
- 音频：`DspSynth` 事件式合成（LCG 噪声+正弦→一阶低通→ADSR→距离衰减→混音钳位），
  游戏钩子：开枪/爆炸/脚步/常开环境风
- 玩法：`Tactic::CoverSeek` 掩体利用；`MapObstacle` HP（Wall150/Block300/Barrier100）可击穿；
  `MissionObjective`（普通=3 波出场总数，压力=歼灭一队）→ 胜利横幅
- 联网：UDP 尽力而为，客户端每 tick 上报 `Input`、服务端每 tick 广播 `Snapshot`（seq+时间戳+
  玩家+NPC），3s/5s 超时；NAT/重连/两机实战场为 TODO

## 5. 待办/下一步（优先级排序）
- **P0 冒烟验证**：`bash scripts/run_gameplay_smoke.sh`（release + X11 包装），
  确认 VUID=0、kills>0、fps 正常、锯齿消失、新功能不破坏站定日志
- **推送**：`git push origin master`（6 个 commit）
- **P1 用户已明确方向**：贴图/美术暂搁置（用户非画师，考虑 AI 生成图；光照渲染验证报告
  `docs/lighting-rendering-verification-2026-08-09.md` 已就绪，结论：当前是片元级 Blinn-Phong
  flat shading，法线贴图/PBR/阴影都是传统光栅特性、dzn 可跑）
- **P1**：Windows 原生 Vulkan 阶段 A（见 `docs/windows-native-vulkan-plan-2026-08-09.md`，
  需 Windows 真机）
- **P2**：64v64 大战场最终验收、AI 行为继续丰富、真爆炸玩法（SoA 布局）、性能日志再扩展

## 6. 上下文节约铁律（沿用）
- 不读 target/、Cargo.lock、*.spv；不反复 cat 同一文件；git diff 一律 --stat 或限定文件；
  AGENTS.md/HANDOFF 是结论缓存
