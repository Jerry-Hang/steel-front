# 交接日志 2026-08-25（LLM 指挥官 / 联机 MVP / 美术立体化 / 代码审查轮）

> 本交接面向上：*下一个接手本项目的 AI 会话*。按主题分节：做了什么 / 关键决策 / 验证结果。

## 〇、本轮概览与 Git 状态
- HEAD = 3d0eeb1（2026-08-25 美术修复）；本日志所涉提交：
  - 952bb80 / 5e5cfd9 / 92f406f / dd06929：网络联机 MVP→断线重连→上行校正+开火指示
  - 0eb927b：代码审查轮（C2 着色器槽位修正 / 复用去重 / 错误路径统一）
  - a2dbcd6 / b66b89d：AK-12M 高模重建 / 围墙压顶 / 树冠二十面体
  - 3d0eeb1：二十面体树冠+球状爆炸+NPC 穿墙推出
- 环境基线：Vulkan 1.3、RTX 5060 Laptop + Ryzen 16C/32T、独显直连下 IMMEDIATE 呈现 + 全局帧率上限（RV3D_FPS，默认 240 / LLM 模式 90）

## 一、LLM 战时指挥官（阶段 A 完成，阶段 B 数据就绪）
**做了什么**
- src/llm_cmd.rs（零依赖）：手写迷你 JSON 解析器 + HTTP/1.1 POST（std::net）+ 后台线程双缓冲 + 严格 schema 校验；红/蓝各一独立上下文窗口（记忆 4 轮，互不共享）
- 集成线：游戏 0.5s 指挥节拍 → 红蓝态势 JSON → LLM（llama-server OpenAI 接口）→ CmdOverride 覆盖各连命令；校验失败/断连 → 启发式回退
- 数据集：data/llm_decisions.jsonl（side/situation/decision/accepted，含被拒样本）；RV3D_LLM=1 启用，RV3D_LLM_INTERVAL=秒
**关键决策**：LLM 只做战役级战略（30-150s/次），执行层全交给 ai_command 三三制/编队/掩体；no_think:true 抑制思考；零依赖铁律拒绝 serde/tokio
**验证**：端到端采纳日志（llmcmd[red/blue]: 命令已采纳）；CPU 推理（llama-server -ngl 0）72s/次、150s 周期稳定；未微调模型格式合规率约 1/3（预期，微调为阶段 B）

## 二、真实联机 MVP（LAN/同机双人可玩）
**做了什么**
- 服务器权威：Join 注册 → NetPlayer 实体（移动/开火/复活/被击杀/超时踢出）；客户端输入（WASD/朝向/开火）由服务器应用到对应远端玩家；主机子弹可击杀远端（胶囊命中+击杀提示+4s 复活）
- 快照：NpcSnapshot（id/pos/facing/hp/team/firing，行 26 字节）——远端玩家用 100000+ 保留 id 区；客户端渲染服务器世界（阵营/开火指示权威来自快照）
- 断线自动重连：Client::reset_connection + retry_join（实测：杀服务器→警告→重启→重握手恢复）
- 上行位置校正：客户端自身上行实体距本地 >3m → 硬对齐（防漂移）
**启动**：RV3D_NET=server|client + RV3D_NET_ADDR（默认 127.0.0.1:27015）；局域网真机联机仅需改地址
**已知待办**：NAT 穿透（异地）、房间列表 UI、弹道客户端预测、观战模式

## 三、美术立体化（低模几何方向，资产延后）
- 树冠：marker 绿色 tint → 二十面体（ICO_POS/ICO_TRI，12 顶点 20 三角）——消除纸帽子观感
- 爆炸/自发光：slot >= EMISSIVE → 二十面体球状（去方块冲击波）
- AK-12M：实心聚合物托 + 品字弹匣连锁 + 加厚比例（去骨架感）；围墙深色压顶
- 着色器槽位修正（C2）：WGSL marker 区 3072→1024（与 renderer 对齐）——之前枪模/爆炸/NPC 材质错位，C2 修复后第一人称枪械材质正确
**待办（用户反馈）**：AI 出生排队站一排（需连级目标铺开 + 火-机动交替打）；墙/建筑细节与透视观感继续

## 四、代码审查轮（六原则落地）
- 复用：guns/ 12 文件 t/rx/rh 收编（-85 行）、net recv_msg、env_truthy/env_f32、SideCtx::new
- 去重：每帧网格克隆改借用、near/far 前缀栈数组、CompanyReport kills 删、shadow_test 删、SPEC_CONTRIB
- 正确性：C2 槽位常量；鲁棒性：renderer .expect→map_err（2 处）；FFI：cpu.rs 缓冲 64B 对齐
- 测试：399/399 全绿（含多轮过期契约修复：tree settle/occluder/level/10s 重置/net 回环）

## 五、环境与运维备注
- 看门狗 scripts/watchdog.ps1（LLM 采集崩溃重启）；llama-server CPU 推理模式最稳
- 截图流：scripts/shot.ps1（DPI PrintWindow）+ rsz3.ps1（缩放 view.png）
- 起游戏最小命令：$env:RV3D_AUTOSTART='1'; $env:RV3D_PRESENT_MODE='immediate'; start target\release\steel-front.exe

## 六、验收基线（勿破坏）
- 399 #[test] 全绿；cargo build --release 0 warning
- 128v128 压力模式 116-240fps；波次/检视/联机三模式无 panic
- 冒烟：RV3D_AUTOSTART 30s 观察 fps 行与无 device/panic
