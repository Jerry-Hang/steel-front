# 交接日志 2026-08-27（AI 战斗深化 / 联机完整化 / 资产导入管线）

> 面向下一个接手本项目的 AI 会话。按主题：做了什么 / 关键决策 / 验证 / 待办。

## 〇、Git 状态
- HEAD = a3a4f3e（AK-12 导入 MVP）；本周期提交序列：
  - 1dfe4f7：AI 战斗（火-机动交替 + 连级目标铺开）
  - dddcd0d：联机完整（协议版本握手/Refuse + NAT 中继 + 枪口焰 + .bat）
  - 3d0eeb1：美术（二十面体树冠/球形爆炸/NPC 穿墙推出）
  - a3a4f3e：资产导入（OBJ/GLB/WIC-PNG + AK-12 模型导入）
  - 此前：952bb80/5e5cfd9/92f406f/dd06929 联机 MVP 链；0eb927b 代码审查

## 一、AI 战斗深化（完成）
- 火-机动交替：Npc.attack_timer 站打 3.4s → reposition（find_cover_points 掩体点或垂直 9m 侧移）→ 到位/5s 超时回站打；换位中不站定（连续移动射击）；换位目标优先于战术目标（advance_npc goal-match 顶层）
- 连级铺开：公司目标横向 -55/0/+55m（company_count≥3 时按索引），正面连不再汇线——实测军情日志 z 跨度 182m
- NPC 穿墙：移动后 resolve_circle_obstacles（MapObstacle 版 AABB 推开，0.45m）
**验证**：399/399；128v128 战位铺开日志实证；116fps 无 panic

## 二、联机完整化（完成）
- 协议版本：SESSION_VERSION=2（Join 携带 u16）；服务器不匹配 → Refuse{reason}（新消息 0x08）；客户端收到 Refuse 停重试（refused_reason）
- NAT 中继：src/bin/rdv.rs（零依赖文本 UDP：REG name port / WHO → ADDR name addr + PUNCH；源 IP=中继观测、端口=载荷声明）；游戏侧 RV3D_NET_RDV + RV3D_NET_NAME：服务器 rdv_register（真实监听套接字，修正临时端口 bug）、客户端 rdv_resolve（5s 超时 + 打洞 PING）
- 远端枪口焰：快照 firing → 客户端与本地 NPC 同链路粒子（可见对面玩家开火）
- .bat：release_dist/game/联机主机.bat + 联机加入.bat（双击即用）
**实测**：中继全链路（房间名→公网地址→直连→握手→快照，客户端 id=1 connected）✓

## 三、资产导入管线（核心：用户取消全程序化限制，改为外部资产）
- **引擎侧**：src/engine/assets.rs（320 行，零依赖）
  - parse_obj：v/vt/vn/f（四边形三角化、负索引兼容、顶点去重 remap）
  - parse_glb：JSON chunk 用 llm_cmd::parse_json_fn（复用！）；BIN accessor componentType 感知（5126=f32/5125=u32/5123=u16——**u32 索引按 f32 读会全部错乱，这是 AK-12 第一次看不见的根因**）；多 mesh/primitives 合并；材质 baseColorFactor
  - gdi_img::load_rgba：GDI+ 原生（GdiplusStartup + GdipLoadImageFromFile + BitmapLockBits）——**BitmapData 真实布局 = Width/Height/Stride/PixelFormat/Scan0/Reserved**（不是 Rect 开头！FFI 错位=访问违例）；负 stride 绝对化；BGRA→RGBA
  - 测试：obj 立方体/三角形 + glb_parses_real_ak12（63283 顶点）+ png_decode_rgba（1600×1000 与 2543×1443 均通过）
- **游戏侧**：load_gun_glb（GameApp.gun_glb 缓存）：归一化（bbox 中心/0.94m 长轴/长轴对齐 +Z（Y 长轴绕 X -90°））→ 相机空间摆放（T×R 顺序！translate(0.25,-0.20,-0.60) × rotY(180°)=枪管朝 -Z）→ 烘焙光照（light (-0.45,0.8,-0.3)/env 0.32/diff 0.78）+ 材质提亮 3.2×（Sketchfab 纯黑 0.057）
- **验证**：检视模式（RV3D_INSPECT=1）AK-12 完整显示（截图确认：枪托/机匣/弹匣/导轨/枪口）

## 四、已知待办（下一步顺序）
1. **第一人称枪显示**：检视✓但 FP 主视角枪不可见——排查 FP 相机约定（FP 视图矩阵 vs 枪模顶点空间；检视路径只重定位不换管线）。先差分：临时把枪放大/挪近（如 (0,0,-0.5)）验证绘制；再对 FP 的视图矩阵核对
2. **场景道具路径**：assets/props.toml + 渲染器场景网格（导入模型摆放进城市/当掩体；可复用枪模的 vertex 管线，需增加世界空间绘制 + 可能与实例场冲突，安全方案：独立网格管线）
3. **贴图采样**：GLB 嵌入贴图（images/bufferView）+ WIC 解码 → per-mesh 纹理绑定（当前 props 用纯色基色，无贴图）
4. **机型归一化参数化**：bake/scale/朝向按模型自适应（当前 AK-12 特化：长轴 Y + 180°Y）——通用模型用 bbox 自动推断即可

## 五、环境备注
- 3D 工具目录：D:\3D_Work（Blender 5.2.1 zip 386MB + Material Maker 1.7 解压完成）；Materialize/PureRef 待用户浏览器下载
- 模型投放位置：assets/guns/ak12.glb（当前唯一导入模型）；后续 props 放 assets/props/
- 网速备忘：GitHub 直连 11KB/s、gh-proxy.com 镜 ~90KB/s（curl --ssl-no-revoke + -C - 断点续传循环脚本 scripts/mm_resume.bat）
- 测试坑：cargo test 并行器 + GDI+ 曾循环崩溃——单测隔离跑（cargo test --release <name>），长任务全部后台化

## 六、验收基线（勿破坏）
- 399 单测全绿 + glb 实测文件测试；build 0 error
- 检视模式（RV3D_INSPECT=1）导入模型完整显示
- 联机三链路（直连/中继/重连）与 128v128 无 panic
