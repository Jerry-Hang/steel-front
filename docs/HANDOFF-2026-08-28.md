# 交接日志 2026-08-28（外部资产管线 / 枪模颜色收官 / 文档双语化）

> 面向下一个接手本项目的 AI 会话。

## 〇、Git 状态
- HEAD = 1498b2b（枪模布局统一 [pos,uv,color]）；本轮提交序列：
  - 60a0897：原模型本色对照 + 首顶点色日志
  - f2b609a：完整烘焙管线（Blender AO/COLOR_0 VEC3）+ AK-12 实装
  - 6d1821f：emissive 区间真 bug（枪槽 75841 误判自发光）
  - 3cac596：属性偏移 offset_of! 显式化
  - faa8d7b / 1498b2b：step 探针（顶点色链路证明）+ 布局统一（彩虹实验证明 GPU 读 @24）

## 一、外部资产管线（完成）
- src/engine/assets.rs：OBJ 解析（v/vt/vn/f 三角化）+ GLB 解析（多 mesh 合并、componentType 感知 accessor（5126/5125/5123）、COLOR_0 VEC3/VEC4、材质 baseColorFactor）+ gdi_img（GDI+ PNG/JPEG → RGBA，BitmapData 布局 = Width/Height/Stride/PF/Scan0/Reserved，负 stride）
- 游戏侧：load_gun_glb（优先 _baked.glb → ak12.glb 回退）；归一化（bbox 中/长轴对齐/Scale）；fp_gun_matrix 共享（view_inv×anchor×scale，程序化+导入共用）
- Blender 无头（scripts/blender_bake.py）：导入 → 顶点色（材质基色 + vertex_color_dirt AO）→ 解父+identity → 导出带 COLOR_0 的 GLB；渲染自检闭环已验证
- 验证：AK-12 GLB（63283 顶点）检视+第一人称显示 ✓；开火击杀（score 460）✓

## 二、枪模颜色收官（关键过程，供继续）
**已证明（探针+实验链）**：
1. 模型本色 = 黑（baseColorFactor 0.057/0.076；Blender 原材质渲染对照图）；烘焙 COLOR_0 = 0.08-0.11（二进制采样）；游戏解析首色 = 0.132（日志）；索引全部在界（单测）——CPU 侧 100% 正确
2. 顶点色链路通：渲染器注入红色 → 红枪；step(0.5, color.r) 探针 → 弹匣黑/机匣红（网格间色差 = 每网格几何差异，非数据问题）
3. **彩虹实验（决定性）**：属性 desc 的 color=@12/uv=@24 对调 → 彩虹枪 → 证明 GPU 实际从 @24 读 color；统一为 [pos, uv, color] 布局（Vertex 结构+转换+属性三处）已提交（1498b2b）
4. **遗留（最后一环）**：统一布局后仍 ~8× 偏亮——怀疑 `input.color × input.fade` 的 fade（或 tint）：下一步在 vs 的 `if (instance_index == 75841u)` 块**之后**、LOD else 分支**之后**强制 `output.fade = 1.0`，并打日志输出 inst.tint 实际值

## 三、待办清单（顺序）
1. **枪模配色收官**（上面第四节——预计 10-20 分钟）：修 vs fade / 验证 tint；目标：机匣黑金属色（0.13 直达片元）
2. 场景道具路径：assets/props.toml + 世界空间网格管线（导入模型摆进城市当掩体/装饰）
3. GLB 嵌入贴图（images/bufferView）→ 采样；金属度/环境反射近似（Sketchfab 金属质感）
4. 多材质/节点矩阵解析（当前单 prim 材质色；节点矩阵目前靠 exporter 阶段应用）

## 四、工具与环境备注
- Blender 5.2.1 位于 D:\3D_Work\blender\blender-5.2.1-windows-x64\blender.exe；Material Maker 1.7 已解压（用户材料工具）
- 枪模排查方法论（已验证）：红注入 → step 探针 → 属性对调 → 日志；构建注意：Get-ChildItem src -Recurse *.rs | touch + Remove-Item exe 防止静默不重编（系统时间快照差异）
- 测试：404 全绿。

## 五、验收基线
- 404 单测全绿；AK-12 导入模型检视/第一人称显示 + 开火
- 联机三链路（直连/中继/重连）；128v128 无 panic
